import React from "react";

import { TextWrapper } from "./styles";

const Error = () => {
    return (
        <TextWrapper>An error has occurred while loading data. Please try again later</TextWrapper>
    );
};

export default Error;
